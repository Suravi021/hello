mkdir folder
cd folder
vi RecordCount.java
vi RecordCountMapper.java 
vi RecordCountReducer.java

javac -classpath $(hadoop classpath) -d folder RecordCount.java RecordCountMapper.java RecordCountReducer.java

jar -cvf recordcount.jar -C folder/ .
cd folder
jar -cvf wordcount.jar *.class 

start-dfs.sh
start-yarn.sh

Starts:

HDFS
YARN (job manager)
hdfs dfs -mkdir /input
hdfs dfs -put dataset124.csv /input/data.csv
yarn jar wordcount.jar RecordCount /input /output

hdfs dfs -cat /output/part-r-00000
